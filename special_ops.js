const abs = (num)  => {return Math.abs(num)}

const ceil = (num)  => {return Math.ceil(num)}

const floor = (num)  => {return Math.floor(num)}

const round = (num)  => {return Math.round(num)}

module.exports = {
    abs,
    ceil,
    floor,
    round
}
